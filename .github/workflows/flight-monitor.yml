name: Configurable Flight Monitor
on:
  schedule:
    # Smart monitoring: Every 4 days at 10 AM UTC (7 runs per month)
    # This provides balanced coverage while staying within quota limits
    - cron: '0 10 */4 * *'   # Every 4th day of the month at 10:00 AM UTC
  
  workflow_dispatch:  # Allow manual runs for testing

jobs:
  flight-monitor:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run automated flight monitor
      env:
        SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
        EMAIL_USER: ${{ secrets.EMAIL_USER }}
        EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
        EMAIL_TO: ${{ secrets.EMAIL_TO }}
      run: |
        echo "🚀 Starting automated flight monitor..."
        python automated_flight_monitor.py
        echo "✅ Flight monitor completed"
    
    - name: Upload backup data
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: flight-data-backup-${{ github.run_number }}
        path: flight_monitor_backup_*.json
        retention-days: 30
    
    - name: Log completion
      run: |
        echo "📊 Flight monitoring job completed at $(date)"
        echo "🔗 Check email for detailed report"
